@model UnsubscribeViewModel
@{
    ViewData["Title"] = "Stop";
    Layout = "_Layout";
}
<div class="text-center">
    <h1 class="display-4">Остановить ведение дневника погоды</h1>
    <p>Выберите дневник, который хотите остановить</p>
    <br><br>
    <form method="post" action="~/Diary/Unsubscribe">
        <table class="center_table">
            @Html.DropDownListFor(model => model.Cities, new SelectList(Model.Cities.OrderBy(c => c)))
        </table>
        <br><br><br><br>
        <input type="submit" value="Остановить данный дневник" />
    </form>
    @if (Model.SuccessMessage != null)
    {
        <br /><br />
        <p class="success_message">@Model.SuccessMessage</p>
    }
</div>